apiVersion: v1
kind: Service
metadata:
  name: httpd-deployment
spec:
  ports:
  - port: 80
    protocol: TCP
  #selector:
    #run: httpd-deployment
  selector:
    app: httpd-deployment
  selector:
    app: httpd-deployment
  type: LoadBalancer     
  
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: httpd-deployment
  labels:
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: httpd-deployment 
    spec:
      containers:
      - name: httpd
        image: tsttst.azurecr.io/httpd:#{user}#
        ports:
        - containerPort: 80
